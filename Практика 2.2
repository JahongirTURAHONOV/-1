#включить "pch.ч"
#include <iostream>
#include <cmath>
#включить ч>
использование пространства имен std;

int main()
{
 SetConsoleCP (1251);
 SetConsoleOutputCP(1251);

 двойной a, b, t, z;
 cout << "Введите числа a и b" << endl;
 cin >>> a >>>>>>> b;
 если (a >= b)
 z = 1-2 * cos (a) * sin(b));
 остальное если (a )
 z = pow (abs (a*a-b * b), (1.0 / 2));
 if (z )
 t = pow ((z + a * a * b), 1.0 / 3);
 еще если (z == b)
 t = 1-log (z) + cos(a*a * b);
 еще
 t = 1 / cos(z*a);
 cout < < "t =" < < t < < endl < < "z =" < < z <;
 система ("пауза");
 возврат 0;
}
