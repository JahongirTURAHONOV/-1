
#включить "pch.
#include <iostream>
#include <cmath>
#включить ч>

использование пространства имен std;

int main()
{
SetConsoleCP (1251);
 SetConsoleOutputCP(1251);

 двойной a, b, n, m;
 cout << "Введите числа a и b" << endl;
 cin >> a >> b;//считывание исходных данных
 если (a >= b)
 n = pow(a - b, (1.0 / 3)); //возведение в степень
 остальное если (a )
 n = a * a + (a - b) / sin(a * b);
 if (n )
 m = (n + a) / - b + pow ((sin(a)*sin (a) - cos (n)), 1.0 / 2);
 еще если (n == b)
 m = b * b + tan(n * a);
 еще
 m = pow (b, 3) + n * a*a;
 cout < < "m =" < < m < < endl < < "n =" < < n <;
 система ("пауза");
 возврат 0;

}
