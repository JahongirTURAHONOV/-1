include <iostream>
#include <ctime>
#include <iomanip>

using namespace std;

void sort(int *a , int n );
void sort(double *a , int n );

int main() 
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	
	srand(time(0));
	int n,z;
	cout<<"Введите кол-во чисел: "<<endl;
	cin >> n;
	cout<<"Дробные числа (0), Целые числа (1): "<<endl;
	cin>>z;
	if (z == 0) {
		double*a = new double[n];
		for (int i = 0; i < n; i++) {
		    cout<< "Введите дробное число: " << endl;
			cin >> a[i];
		}
		sort(a, n);

		for (int i = 0; i < n; i++)
			cout << a[i] << endl;
	}
	else {
		int*a = new int[n];
		for (int i = 0; i < n; i++) {
		     cout <<"Введите целое число: "<<endl;
			cin >> a[i];
		}
		sort(a, n);

		for (int i = 0; i < n; i++)
			cout << a[i] << endl;
	}
	system("pause");
	return 0;
}
void sort(double *a, int n)
{
	int i, j, tmp;
cout<<"Дробные числа сортируются"<< endl;
	for (i = 0; i < n; i++)
		for (j = n - 1; j >= 0; j--)
			if (a[j] < a[j + 1])
			{
				tmp = a[j];
				a[j] = a[j + 1];
				a[j + 1] = tmp;
			}
cout << "Окончание сортировки" << endl;
}

void sort(int *a, int n)
{
	int i, j, tmp;
cout<<"Целые числа сортируются"<<endl;
	for (i = 0; i < n; i++)
		for (j = n - 1; j >= 0; j--)
			if (a[j] < a[j + 1])
			{
				tmp = a[j];
				a[j] = a[j + 1];
				a[j + 1] = tmp;
			}
cout << "Окончание сортировки" << endl;
}
