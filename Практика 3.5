#включить "pch.ч"
#include <iostream>
#include <string>
#include " windows.ч"
#include <cstdio>
использование пространства имен std;

class library { //Объявляемсам класс
общественный:
 string style;//Объявляем переменные
 автор строки;
 заголовок строки;
 int год;
 int цена;

 string *members;//объявление переменных

 int n;

 void set () / / ввод
	{
 cout << "Введите количество книг:" << endl;
 cin >>> n;
 members = новая строка[n];

 for (int i = 0; i )
		{
 cout << "Введите жанр_ФИО_название_год книги №" << i + 1 << ":" << endl;
 cin > > > участники[i];

		}
	}
 void show()//вывод
	{
 cout < < endl << "-----------------------------------------------------" <;
 for (int i = 0; i )
		{
 cout << "книга №" << i + 1 << ":" << members[i] << endl;

		}

	}
};


int main()
{
 SetConsoleCP (1251);
 SetConsoleOutputCP(1251);
 setlocale (LC_ALL, " Rus");
 библиотека x;

 x.набор();
 x.показать();

 система ("пауза");
 возврат 0;
}
